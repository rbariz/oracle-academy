
  CREATE TABLE "ATTENDANCE"
   (	"ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE,
	"MEETING_ID" NUMBER NOT NULL ENABLE,
	"ENROLLMENT_ID" NUMBER NOT NULL ENABLE,
	"STATUS_CODE" VARCHAR2(30 BYTE) DEFAULT 'PRESENT' NOT NULL ENABLE,
	"COMMENTS" VARCHAR2(1000 BYTE),
	"RECORDED_AT" TIMESTAMP (6) DEFAULT SYSTIMESTAMP NOT NULL ENABLE
   ) ;
ALTER TABLE "ATTENDANCE" ADD CONSTRAINT "CK_ATTENDANCE_STATUS" CHECK (
            status_code IN (
                'PRESENT',
                'ABSENT',
                'LATE',
                'EXCUSED'
            )
        ) ENABLE;
ALTER TABLE "ATTENDANCE" ADD CONSTRAINT "PK_ATTENDANCE" PRIMARY KEY ("ID")
  USING INDEX  ENABLE;
ALTER TABLE "ATTENDANCE" ADD CONSTRAINT "UK_ATTENDANCE" UNIQUE ("MEETING_ID", "ENROLLMENT_ID")
  USING INDEX  ENABLE;
ALTER TABLE "ATTENDANCE" ADD CONSTRAINT "FK_ATTENDANCE_MEETING" FOREIGN KEY ("MEETING_ID")
	  REFERENCES "SESSION_MEETING" ("ID") ON DELETE CASCADE ENABLE;
ALTER TABLE "ATTENDANCE" ADD CONSTRAINT "FK_ATTENDANCE_ENROLLMENT" FOREIGN KEY ("ENROLLMENT_ID")
	  REFERENCES "ENROLLMENT" ("ID") ON DELETE CASCADE ENABLE;

