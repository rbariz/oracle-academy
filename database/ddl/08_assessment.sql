
  CREATE TABLE "ASSESSMENT"
   (	"ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE,
	"ENROLLMENT_ID" NUMBER NOT NULL ENABLE,
	"ASSESSMENT_TYPE" VARCHAR2(30 BYTE) NOT NULL ENABLE,
	"ASSESSMENT_DATE" DATE,
	"SCORE" NUMBER(5,2),
	"COMMENTS" VARCHAR2(1000 BYTE),
	"CREATED_AT" TIMESTAMP (6) DEFAULT SYSTIMESTAMP NOT NULL ENABLE
   ) ;
ALTER TABLE "ASSESSMENT" ADD CONSTRAINT "CK_ASSESSMENT_TYPE" CHECK (
            assessment_type IN (
                'QUIZ',
                'PRACTICAL',
                'FINAL'
            )
        ) ENABLE;
ALTER TABLE "ASSESSMENT" ADD CONSTRAINT "CK_ASSESSMENT_SCORE" CHECK (
            score IS NULL
            OR score BETWEEN 0 AND 100
        ) ENABLE;
ALTER TABLE "ASSESSMENT" ADD CONSTRAINT "PK_ASSESSMENT" PRIMARY KEY ("ID")
  USING INDEX  ENABLE;
ALTER TABLE "ASSESSMENT" ADD CONSTRAINT "FK_ASSESSMENT_ENROLLMENT" FOREIGN KEY ("ENROLLMENT_ID")
	  REFERENCES "ENROLLMENT" ("ID") ON DELETE CASCADE ENABLE;

